{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install && PLAYWRIGHT_BROWSERS_PATH=/app/pw-browsers npx playwright install chromium && npm run build"
  },
  "deploy": {
    "startCommand": "next start -p ${PORT:-3000}",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
